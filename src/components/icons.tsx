import {
  Home,
  FlaskConical,
  Terminal,
  GraduationCap,
  Contact,
} from "lucide-react"

export const Icons = {
  home: Home,
  contact: Contact,
  exp: FlaskConical,
  learning: GraduationCap,
  code: Terminal,
}